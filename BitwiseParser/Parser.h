#pragma once
#include <bitset>

class Parser {
public:
		template<int Size>
		static std::bitset<Size> getBitset(const std::string& input)
		{
			int content = std::stoi(input);
			int bits = 0;
			bool isNegative = false;

			//TODO: bitset getter.
			
		}

		static std::string bitSetStr(const std::string& input)
		{
			
				

			
			
		}
};